name: GlobalStockNow Full Pipeline

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 속보 수집 실행
        run: |
          pip install feedparser
          python collector.py

      - name: 속보 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: breaking-news-${{ github.run_number }}
          path: breaking_news.json

      - name: AI 분석 실행 (안정화 버전)
        run: |
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install transformers accelerate sentencepiece
          python analyzer.py || echo "AI 분석 중 오류 발생 - 빈 결과로 대체"
          # 실패해도 analyzed_news.json 생성
          if [ ! -f analyzed_news.json ]; then
            echo "[]" > analyzed_news.json
          fi

      - name: AI 분석 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: analyzed-news-${{ github.run_number }}
          path: analyzed_news.json
          
      - name: 텔레그램으로 분석 결과 전송
        if: always()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=HTML \
            -d text="<b>🌍 글로벌스톡나우 속보 브리핑 (#${{ github.run_number }})</b>%0A%0A$(jq -r '.[] | \"• <a href=\\\"\(.original_link)\\\">\\(.title)</a>%0A영향도: \\(.impact_score)/10%0A종목: \\(.korean_stocks | join(\", \"))%0A이유: \\(.reason)%0A%0A\"' analyzed_news.json 2>/dev/null || echo '오늘은 주요 속보 없음')%0A%0A<i>AI 자동 분석 완료 - 글로벌스톡나우</i>"
