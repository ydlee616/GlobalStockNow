name: GlobalStockNow Collector

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'

jobs:
  collector-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 속보 수집 실행
        run: |
          echo "🚀 GlobalStockNow 속보 수집 시작 ($(date +'%Y-%m-%d %H:%M UTC'))"
          pip install feedparser fuzzywuzzy python-levenshtein
          python collector.py
          echo "✅ 속보 수집 완료! breaking_news.json 생성됨"

      - name: 속보 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: breaking-news-${{ github.run_number }}
          path: breaking_news.json

      - name: 완료 알림
        run: |
          echo "🌟 속보 수집 성공! Artifact에서 JSON 다운로드 가능"
