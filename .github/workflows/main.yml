name: GlobalStockNow Full Pipeline

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'  # 매 4시간 자동 실행

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 속보 수집 실행 (RSS + X 실시간, 오류 무시)
        run: |
          pip install feedparser snscrape --quiet || echo "snscrape 설치 실패 무시"
          python collector.py || echo "collector 실패 무시 - 빈 파일 생성"
          touch breaking_news.json
          echo "[]" > breaking_news.json || true

      - name: 속보 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: breaking-news-${{ github.run_number }}
          path: breaking_news.json

      - name: AI 분석 실행 (경량 모델 + 오류 무시)
        run: |
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu --quiet
          pip install transformers accelerate sentencepiece --quiet
          python analyzer.py || echo "analyzer 실패 무시 - 빈 파일 생성"
          touch analyzed_news.json
          echo "[]" > analyzed_news.json || true

      - name: AI 분석 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: analyzed-news-${{ github.run_number }}
          path: analyzed_news.json

      - name: 텔레그램으로 분석 결과 전송 (영향도 3점 이상 포함)
        if: always()
        run: |
          HIGHS=$(jq -r '.[] | select(.impact_score >= 3) | "• <a href=\\\"\(.original_link)\\\">\\(.title)</a>%0A영향도: \\(.impact_score)/10%0A종목: \\(.korean_stocks | join(\", \") // "없음")%0A이유: \\(.reason)%0A%0A"' analyzed_news.json 2>/dev/null || echo "")
          if [ -z "$HIGHS" ]; then
            HIGHS="오늘은 영향도 3점 이상 주요 속보 없음"
          fi
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=HTML \
            -d text="<b>🌍 글로벌스톡나우 속보 브리핑 (#${{ github.run_number }})</b>%0A%0A$HIGHS%0A<i>AI 자동 분석 완료 - 글로벌스톡나우</i>"
