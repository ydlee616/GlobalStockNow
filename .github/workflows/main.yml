name: GlobalStockNow Pipeline

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  globalstocknow-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 속보 수집 실행
        run: |
          echo "🚀 [$(date +'%Y-%m-%d %H:%M UTC')] 속보 수집 시작"
          pip install feedparser fuzzywuzzy python-levenshtein
          python collector.py

      - name: 속보 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: breaking-news-${{ github.run_number }}
          path: breaking_news.json

      - name: AI 분석 실행 (Qwen2.5-1.5B)
        run: |
          echo "🧠 [$(date +'%Y-%m-%d %H:%M UTC')] AI 분석 시작 - 패키지 설치"
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install transformers accelerate bitsandbytes sentencepiece
          echo "✅ 패키지 설치 완료 - 분석 실행"
          python analyzer.py

      - name: AI 분석 결과 저장
        uses: actions/upload-artifact@v4
        with:
          name: analyzed-news-${{ github.run_number }}
          path: analyzed_news.json

      - name: 완료 알림
        run: |
          echo "🌟 GlobalStockNow 파이프라인 성공! ($(date +'%Y-%m-%d %H:%M UTC'))"
          echo "속보 수집 + AI 분석 완료"
